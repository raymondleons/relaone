const initialState={
    intro: 'RelaOne'
}

const landingPage=(state=initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default landingPage;


